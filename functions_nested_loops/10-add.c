#include "main.h"
/**
* add - adds two integers and returns the result
* @i: first integer
* @c: second integer
*Return: The result of the addition.
*/
int add(int i, int c)
{
	int result;

	result = i + c;
	return (result);
}
