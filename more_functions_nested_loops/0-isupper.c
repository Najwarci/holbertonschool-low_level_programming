#include "main.h"

/**
 * _isupper - Checks if a character is a lowercase letter
 * @c: The character to test
 *
 * Return: 1 if c is a lowercase letter, 0 otherwise
 */

int _isupper(int c)
{
	if (isupper(c))
	{
	return (1);
	}
	else
	{
	return (0);
	}
}
