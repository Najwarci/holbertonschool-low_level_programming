#include "main.h"

/**
 * _isdigit - Checks if a digit
 * @c: The character to test
 *
 * Return: 1 if c is a digit
 */

int _isdigit(int c)
{
	if (isdigit(c))
	{
	return (1);
	}
	else
	{
	return (0);
	}
}
