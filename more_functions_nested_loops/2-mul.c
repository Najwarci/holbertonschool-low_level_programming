#include "main.h"
/**
* mul - multiplies two integers and returns the result
* @i: first integer
* @c: second integer
*Return: The result of the addition.
*/
int mul(int i, int c)
{
	int result;

	result = i * c;
	return (result);
}
